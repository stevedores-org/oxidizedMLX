[package]
name = "mlx-cpu"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure Rust CPU backend for MLX (correctness oracle)"

[dependencies]
mlx-core = { path = "../mlx-core" }
